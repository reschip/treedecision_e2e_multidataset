# (#9) Pinned exact versions - prevents silent breaking changes on re-install.
# Last verified working: 2026-06-30
# To upgrade: test in a fresh venv, then update ALL versions together.

# -- Web framework -------------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
pydantic-settings==2.3.0       # (#env) leer variables de entorno con type safety
python-multipart==0.0.9

# -- Rate limiting -------------------------------------------------------------
slowapi==0.1.9                  # (#rate-limit) limites por IP en endpoints FastAPI

# -- ML / Data -----------------------------------------------------------------
scikit-learn==1.4.2
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2

# -- Testing -------------------------------------------------------------------
httpx==0.27.0
pytest==8.2.0
pytest-asyncio==0.23.6

# -- Utilidades ----------------------------------------------------------------
pyyaml==6.0.1
